[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pdt-observer"
version = "0.1.0"
description = "OASIS human-AI workbench for observation acquisition and spatial synthesis."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10",
]

[project.optional-dependencies]
api-agent = [
    "openai-agents>=0.6.0",
]
app = [
    "starlette>=0.40",
    "uvicorn>=0.30",
]
dev = [
    "mypy>=1.13",
    "openai-agents>=0.6.0",
    "pytest>=8.3",
    "ruff>=0.8",
    "starlette>=0.40",
    "uvicorn>=0.30",
]

[project.scripts]
pdt-observer = "pdt_observer.cli:console"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pdt_observer = ["static/*.jpg"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
markers = [
    "live: tests that may call external services and require explicit opt-in",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
mypy_path = "src"
packages = ["pdt_observer"]
strict = true
warn_unused_ignores = true
